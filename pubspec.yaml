name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0 # Tambahkan ini
  firebase_ml_vision: ^0.12.0+3 # Version Firebase
  camera: ^0.10.0+1
  image_picker: ^0.8.7+4
  http: ^0.13.3

  english_words: ^4.0.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
